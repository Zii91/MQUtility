{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build MQChecker (Debug)",
      "type": "shell",
      "command": "bazel build //src/main:MQChecker -c dbg --coverage",
      "osx": {
        "command": "bazel build //src/main:MQChecker -c dbg --spawn_strategy=standalone --coverage",
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
    }
  ]
}
